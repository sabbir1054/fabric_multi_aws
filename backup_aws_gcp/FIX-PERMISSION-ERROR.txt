═══════════════════════════════════════════════════════════════════
                    🔴 PERMISSION DENIED ERROR
═══════════════════════════════════════════════════════════════════

ERROR YOU GOT:
  mv: cannot move '/tmp/genesis_fresh.block' to
  './system-genesis-block/genesis.block/genesis_fresh.block':
  Permission denied

WHAT THIS MEANS:
  The genesis.block exists as a DIRECTORY (from previous runs)
  and has wrong permissions. Need to clean it properly.

═══════════════════════════════════════════════════════════════════
                    ✅ THE FIX (Run This Now)
═══════════════════════════════════════════════════════════════════

STEP 1: Fix permissions first
────────────────────────────────────────────────────────────────────
./fix-permissions.sh


STEP 2: Then run fresh start
────────────────────────────────────────────────────────────────────
./FRESH-START.sh


═══════════════════════════════════════════════════════════════════
                    📊 WHAT WAS FIXED
═══════════════════════════════════════════════════════════════════

✅ FRESH-START.sh updated to:
   • Use sudo to remove old directories
   • Set proper permissions (chmod 777) on directories
   • Remove genesis.block before moving new one
   • Clean channel-artifacts properly

✅ fix-permissions.sh created to:
   • Clean system-genesis-block directory
   • Clean channel-artifacts directory
   • Remove temp files
   • Set proper permissions

═══════════════════════════════════════════════════════════════════
                    🚀 RUN THESE COMMANDS NOW
═══════════════════════════════════════════════════════════════════

chmod +x fix-permissions.sh
./fix-permissions.sh

chmod +x FRESH-START.sh
./FRESH-START.sh


═══════════════════════════════════════════════════════════════════
                    OR - MANUAL FIX
═══════════════════════════════════════════════════════════════════

If scripts don't work, run these manually:

sudo rm -rf ./system-genesis-block
mkdir -p ./system-genesis-block
sudo chmod 777 ./system-genesis-block

sudo rm -rf ./channel-artifacts
mkdir -p ./channel-artifacts
sudo chmod 777 ./channel-artifacts

Then run: ./FRESH-START.sh


═══════════════════════════════════════════════════════════════════

QUICK FIX: ./fix-permissions.sh && ./FRESH-START.sh

═══════════════════════════════════════════════════════════════════
