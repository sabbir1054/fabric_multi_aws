version: "2.4"

networks:
  fabric-net:

volumes:
  ordererdata:

services:

  # -----------------------------
  # ORDERER (AWS)
  # -----------------------------
  orderer.example.com:
    container_name: orderer.example.com
    image: hyperledger/fabric-orderer:2.4

    environment:
      - ORDERER_GENERAL_LOGLEVEL=info
      - ORDERER_GENERAL_LISTENADDRESS=0.0.0.0
      - ORDERER_GENERAL_LISTENPORT=7050

      - ORDERER_GENERAL_GENESISMETHOD=file
      - ORDERER_GENERAL_GENESISFILE=/var/hyperledger/orderer/genesis.block

      - ORDERER_GENERAL_LOCALMSPID=OrdererMSP
      - ORDERER_GENERAL_LOCALMSPDIR=/var/hyperledger/orderer/msp

      # ---- TLS CONFIG ----
      - ORDERER_GENERAL_TLS_ENABLED=true
      - ORDERER_GENERAL_TLS_PRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_GENERAL_TLS_CERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_GENERAL_TLS_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]

    working_dir: /opt/hyperledger/orderer

    volumes:
      # THIS MUST BE A FILE (not folder)
      - ./system-genesis-block/genesis.block:/var/hyperledger/orderer/genesis.block:ro

      # MSP + TLS folders
      - ./organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp:/var/hyperledger/orderer/msp:ro
      - ./organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls:/var/hyperledger/orderer/tls:ro

      # Orderer ledger
      - ordererdata:/var/hyperledger/production/orderer

    ports:
      - "7050:7050"

    networks:
      - fabric-net

  # -----------------------------
  # PEER ORG1
  # -----------------------------
  peer0.org1.example.com:
    container_name: peer0.org1.example.com
    image: hyperledger/fabric-peer:2.4

    environment:
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_PEER_ID=peer0.org1.example.com

      - CORE_PEER_ADDRESS=peer0.org1.example.com:7051
      - CORE_PEER_LISTENADDRESS=0.0.0.0:7051

      - CORE_PEER_LOCALMSPID=Org1MSP

      # Gossip
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=peer0.org1.example.com:7051

      # TLS
      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_TLS_KEY_FILE=/var/hyperledger/peer/tls/server.key
      - CORE_PEER_TLS_CERT_FILE=/var/hyperledger/peer/tls/server.crt
      - CORE_PEER_TLS_ROOTCERT_FILE=/var/hyperledger/peer/tls/ca.crt

    working_dir: /opt/hyperledger/peer

    volumes:
      - /var/run/:/host/var/run/
      - ./organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/msp:/var/hyperledger/peer/msp:ro
      - ./organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/tls:/var/hyperledger/peer/tls:ro
      - ./organizations/peerOrganizations/org1.example.com/users:/var/hyperledger/peer/users

      # chaincode workspace
      - ./chaincode:/opt/gopath/src/github.com/chaincode

    ports:
      - "7051:7051"
      - "7053:7053"

    networks:
      - fabric-net

  # -----------------------------
  # CLI
  # -----------------------------
  cli:
    container_name: cli
    image: hyperledger/fabric-tools:2.4
    tty: true
    stdin_open: true
    working_dir: /opt/gopath/src/github.com/hyperledger/fabric/peer

    environment:
      - FABRIC_CFG_PATH=/etc/hyperledger/fabric

    volumes:
      - ./organizations:/opt/gopath/src/github.com/hyperledger/fabric/peer/organizations
      - ./channel-artifacts:/opt/gopath/src/github.com/hyperledger/fabric/peer/channel-artifacts
      - ./chaincode:/opt/gopath/src/github.com/chaincode

      # configtx.yaml for channel create
      - ./configtx:/opt/hyperledger/fabric/configtx
      - ./configtx/configtx.yaml:/etc/hyperledger/fabric/configtx.yaml

    networks:
      - fabric-net